# Office CPU/GPU parity manifest for representative nonlinear lanes.

[lane.cyclone_miller]
config = "../examples/configs/runtime_cyclone_nonlinear_gx_miller.toml"
steps = 8
fixed_dt = true
required_nonzero = ["state_norm", "Wphi"]

[lane.w7x_vmec]
config = "../examples/configs/runtime_w7x_nonlinear_vmec_geometry.toml"
steps = 4
fixed_dt = true
required_nonzero = ["state_norm", "Wphi"]

