name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Verify release version
        run: |
          python tools/release/check_release_readiness.py version \
            --tag "${GITHUB_REF_NAME}" \
            --require-tag \
            --check-pypi

      - name: Release readiness gates
        run: |
          python tools/release/check_repository_size_manifest.py
          python tools/release/check_repository_size_manifest.py release-artifacts
          python tools/release/check_package_architecture_manifest.py
          python tools/release/check_package_architecture_manifest.py differentiable-refactor
          python tools/release/check_parallel_scaling_artifacts.py --performance-manifest-only
          python tools/release/check_parallel_scaling_artifacts.py
          python tools/release/check_quasilinear_promotion_guardrails.py \
            --out-json /tmp/quasilinear_promotion_guardrails.json
          python tools/release/check_vmec_boozer_gates.py differentiability-claim \
            --out-json docs/_static/vmec_boozer_differentiability_claim_guard.json
          python tools/artifacts/build_parallelization_completion_status.py \
            --out-prefix /tmp/parallelization_completion_status \
            --skip-figures
          python tools/release/check_release_readiness.py technical-status \
            --out-json /tmp/technical_release_status.json \
            --fail-under 98
          python tools/release/check_release_readiness.py \
            --out-json /tmp/release_readiness.json

      - name: Build artifacts (sdist + wheel)
        run: |
          python -m pip install --upgrade pip build twine
          rm -rf build dist
          python -m build

      - name: Check distribution metadata
        run: |
          twine check dist/*

      - name: Smoke-test installed wheel
        run: |
          python -m venv /tmp/spectraxgk-wheel-smoke
          /tmp/spectraxgk-wheel-smoke/bin/pip install dist/*.whl
          cd /tmp
          /tmp/spectraxgk-wheel-smoke/bin/python - <<'PY'
          import spectraxgk
          import spectraxgk.parallel
          from importlib.util import find_spec
          from spectraxgk.cli import build_parser

          assert "--plot" in build_parser().format_help()
          assert "batch_map" in spectraxgk.parallel.__all__
          assert spectraxgk.parallel.batch_map.__module__ == "spectraxgk.parallel.batch"
          assert find_spec("spectraxgk.objectives.qa_low_turbulence_contracts") is None
          assert find_spec("spectraxgk.objectives.qa_low_turbulence_residuals") is None
          assert find_spec("spectraxgk.objectives.stellarator_residuals") is None
          assert find_spec("spectraxgk.diagnostics.growth_series") is None
          print(spectraxgk.__version__)
          PY
          /tmp/spectraxgk-wheel-smoke/bin/spectraxgk --help >/dev/null
          /tmp/spectraxgk-wheel-smoke/bin/spectrax-gk --help >/dev/null

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
